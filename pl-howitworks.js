/* Power Ladder - How It Works (Wix Custom Code build) */
window.PL_SIM={"rows":[{"scenario":"S1","scenario_label":"Base plan","approach":"Current approach (proxy)","opportunity_score":55.05182560097535,"financial_readiness_score":49.55555555555556,"spend_thb":877000.0,"planned_gross_profit_thb":367729.51,"cash_after_purchase_thb":223000.0,"quick_ratio_post":0.4955555555555556,"orders":[15,12,50]},{"scenario":"S1","scenario_label":"Base plan","approach":"AI alone","opportunity_score":100.0,"financial_readiness_score":52.11111111111111,"spend_thb":865500.0,"planned_gross_profit_thb":684922.9569999999,"cash_after_purchase_thb":234500.0,"quick_ratio_post":0.5211111111111111,"orders":[52,35,22]},{"scenario":"S1","scenario_label":"Base plan","approach":"Business Play Strategist + AI","opportunity_score":89.27460961559052,"financial_readiness_score":75.0,"spend_thb":762500.0,"planned_gross_profit_thb":615877.155,"cash_after_purchase_thb":337500.0,"quick_ratio_post":0.75,"orders":[51,35,15]},{"scenario":"S2","scenario_label":"Growth plan","approach":"Current approach (proxy)","opportunity_score":52.0991897791008,"financial_readiness_score":56.875,"spend_thb":877000.0,"planned_gross_profit_thb":409603.15,"cash_after_purchase_thb":273000.0,"quick_ratio_post":0.56875,"orders":[15,12,50]},{"scenario":"S2","scenario_label":"Growth plan","approach":"AI alone","opportunity_score":100.0,"financial_readiness_score":26.5625,"spend_thb":1022500.0,"planned_gross_profit_thb":808882.1725,"cash_after_purchase_thb":127500.0,"quick_ratio_post":0.265625,"orders":[62,41,26]},{"scenario":"S2","scenario_label":"Growth plan","approach":"Business Play Strategist + AI","opportunity_score":78.36422651760195,"financial_readiness_score":76.04166666666666,"spend_thb":785000.0,"planned_gross_profit_thb":644000.0,"cash_after_purchase_thb":365000.0,"quick_ratio_post":0.7604166666666666,"orders":[61,40,10]},{"scenario":"S3","scenario_label":"Demand lift","approach":"Current approach (proxy)","opportunity_score":51.693890132903064,"financial_readiness_score":58.599999999999994,"spend_thb":877000.0,"planned_gross_profit_thb":409695.46,"cash_after_purchase_thb":293000.0,"quick_ratio_post":0.586,"orders":[15,12,50]},{"scenario":"S3","scenario_label":"Demand lift","approach":"AI alone","opportunity_score":100.0,"financial_readiness_score":28.499999999999996,"spend_thb":1027500.0,"planned_gross_profit_thb":815565.1259999999,"cash_after_purchase_thb":142500.0,"quick_ratio_post":0.285,"orders":[63,41,26]},{"scenario":"S3","scenario_label":"Demand lift","approach":"Business Play Strategist + AI","opportunity_score":78.22606638289126,"financial_readiness_score":76.0,"spend_thb":790000.0,"planned_gross_profit_thb":648000.0,"cash_after_purchase_thb":380000.0,"quick_ratio_post":0.76,"orders":[62,40,10]},{"scenario":"S4","scenario_label":"Cash squeeze","approach":"Current approach (proxy)","opportunity_score":53.66955482458926,"financial_readiness_score":22.115384615384613,"spend_thb":765000.0,"planned_gross_profit_thb":368181.45999999996,"cash_after_purchase_thb":115000.0,"quick_ratio_post":0.22115384615384615,"orders":[15,12,42]},{"scenario":"S4","scenario_label":"Cash squeeze","approach":"AI alone","opportunity_score":88.54946222637598,"financial_readiness_score":20.0,"spend_thb":776000.0,"planned_gross_profit_thb":627748.9975,"cash_after_purchase_thb":104000.0,"quick_ratio_post":0.2,"orders":[52,36,15]},{"scenario":"S4","scenario_label":"Cash squeeze","approach":"Business Play Strategist + AI","opportunity_score":61.860919855352094,"financial_readiness_score":67.11538461538461,"spend_thb":531000.0,"planned_gross_profit_thb":449748.9975,"cash_after_purchase_thb":349000.0,"quick_ratio_post":0.6711538461538461,"orders":[45,36,0]},{"scenario":"S5","scenario_label":"Stabilize","approach":"Current approach (proxy)","opportunity_score":54.287867354862584,"financial_readiness_score":36.041666666666664,"spend_thb":877000.0,"planned_gross_profit_thb":357708.6,"cash_after_purchase_thb":173000.0,"quick_ratio_post":0.36041666666666666,"orders":[15,12,50]},{"scenario":"S5","scenario_label":"Stabilize","approach":"AI alone","opportunity_score":100.0,"financial_readiness_score":41.354166666666664,"spend_thb":851500.0,"planned_gross_profit_thb":675455.3055,"cash_after_purchase_thb":198500.0,"quick_ratio_post":0.41354166666666664,"orders":[52,35,21]},{"scenario":"S5","scenario_label":"Stabilize","approach":"Business Play Strategist + AI","opportunity_score":83.87857061285892,"financial_readiness_score":72.29166666666667,"spend_thb":703000.0,"planned_gross_profit_thb":572758.448,"cash_after_purchase_thb":347000.0,"quick_ratio_post":0.7229166666666667,"orders":[52,34,11]},{"scenario":"S6","scenario_label":"Expansion","approach":"Current approach (proxy)","opportunity_score":53.009985855098115,"financial_readiness_score":91.75438596491227,"spend_thb":877000.0,"planned_gross_profit_thb":381282.82999999996,"cash_after_purchase_thb":523000.0,"quick_ratio_post":0.9175438596491228,"orders":[15,12,50]},{"scenario":"S6","scenario_label":"Expansion","approach":"AI alone","opportunity_score":100.0,"financial_readiness_score":80.0,"spend_thb":944000.0,"planned_gross_profit_thb":738634.5395,"cash_after_purchase_thb":456000.0,"quick_ratio_post":0.8,"orders":[57,38,24]},{"scenario":"S6","scenario_label":"Expansion","approach":"Business Play Strategist + AI","opportunity_score":99.53030617037152,"financial_readiness_score":83.94736842105263,"spend_thb":921500.0,"planned_gross_profit_thb":735131.312,"cash_after_purchase_thb":478500.0,"quick_ratio_post":0.8394736842105263,"orders":[57,37,23]}],"status":"ILLUSTRATIVE_SIMULATION_NOT_MEASURED_RESULTS","monetary_scale_factor":10,"scenarios":[{"id":"S1","label":"Base plan","forecast":[51.329073,34.916954,21.772951],"cash":1100000,"current_liabilities":450000,"known_promotion":0},{"id":"S2","label":"Growth plan","forecast":[61.485735,40.444811,25.960315],"cash":1150000,"current_liabilities":480000,"known_promotion":1},{"id":"S3","label":"Demand lift","forecast":[62.437589,40.815908,25.969546],"cash":1170000,"current_liabilities":500000,"known_promotion":1},{"id":"S4","label":"Cash squeeze","forecast":[53.983308,35.966533,21.818146],"cash":880000,"current_liabilities":520000,"known_promotion":0},{"id":"S5","label":"Stabilize","forecast":[51.939612,34.665101,20.77086],"cash":1050000,"current_liabilities":480000,"known_promotion":0},{"id":"S6","label":"Expansion","forecast":[56.907828,37.296053,23.128283],"cash":1400000,"current_liabilities":570000,"known_promotion":1}]};
(function(){var r=document.getElementById('plhw');if(!r)return;
r.insertAdjacentHTML('beforeend',"<dialog id=\"image-viewer\" aria-labelledby=\"image-viewer-title\"><div class=\"viewer-top\"><h2 id=\"image-viewer-title\">Business Play product screen</h2><button id=\"close-viewer\" type=\"button\" aria-label=\"Close enlarged image\">Close \u00d7</button></div><img id=\"viewer-image\" alt=\"\"><p id=\"viewer-caption\"></p><a id=\"viewer-original\" target=\"_blank\" rel=\"noopener noreferrer\">Open full-size image \u2197</a></dialog>");
[].forEach.call(r.querySelectorAll('a[data-lightbox]'),function(a){var i=a.querySelector('img');if(i&&!a.getAttribute('href'))a.href=i.getAttribute('src')});
function hide(){['PAGES_CONTAINER','SITE_FOOTER'].forEach(function(id){var n=document.getElementById(id);if(n)n.style.setProperty('display','none','important')})}
hide();var n=0,t=setInterval(function(){hide();if(++n>40)clearInterval(t)},200)})();
'use strict';

const model = window.PL_SIM;
const policies = [
  { name: 'Current approach (proxy)', short: 'Current approach', color: '#68788c', dash: '6 4', symbol: 'circle' },
  { name: 'AI alone', short: 'AI alone', color: '#098cbe', dash: '2 5', symbol: 'square' },
  { name: 'Business Play Strategist + AI', short: 'Strategist + AI', color: '#178565', dash: '', symbol: 'diamond' }
];
const state = { scenario: 'S1', view: 'curves', policy: null };
const byId = id => document.getElementById(id);
const esc = text => String(text).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
const money = n => '฿' + Number(n).toLocaleString('en-US', { maximumFractionDigits: 0 });
const score = n => Number(n).toFixed(1);
const rowFor = (scenario, policy) => model.rows.find(r => r.scenario === scenario && r.approach === policies[policy].name);
const selectedRows = () => policies.map((_, i) => rowFor(state.scenario, i));
const pointY = (n, top, height) => top + (100 - n) / 100 * height;

function markerShape(policy, x, y, size = 5) {
  if (policies[policy].symbol === 'square') return `<rect x="${x-size}" y="${y-size}" width="${size*2}" height="${size*2}" rx="1"/>`;
  if (policies[policy].symbol === 'diamond') return `<path d="M ${x} ${y-size-1} L ${x+size+1} ${y} L ${x} ${y+size+1} L ${x-size-1} ${y} Z"/>`;
  return `<circle cx="${x}" cy="${y}" r="${size}"/>`;
}

function pointMarkup(row, policy, x, y, context, showLabel) {
  const id = `${context}-${row.scenario}-${policy}`;
  const label = `${row.scenario}, ${policies[policy].short}: Opportunity ${score(row.opportunity_score)}, FRS ${score(row.financial_readiness_score)}. Select this approach and scenario.`;
  const selected = state.scenario === row.scenario;
  const isMap = context === 'map';
  const metric = context === 'os' ? row.opportunity_score : row.financial_readiness_score;
  const labelY = y < 35 ? y + 21 + policy * 5 : y - 12 - policy * 3;
  return `<g id="${id}" class="point" data-scenario="${row.scenario}" data-policy="${policy}" tabindex="0" role="button" aria-label="${esc(label)}" aria-describedby="tooltip" style="color:${policies[policy].color}"><circle cx="${x}" cy="${y}" r="20" fill="transparent"/><circle class="ring" cx="${x}" cy="${y}" r="12"/><g fill="${policies[policy].color}" stroke="white" stroke-width="1.5">${markerShape(policy, x, y, selected ? 6 : 4.5)}</g>${showLabel ? `<text x="${x}" y="${labelY}" text-anchor="${x > 415 ? 'end' : x < 55 ? 'start' : 'middle'}" fill="${policies[policy].color}">${isMap ? `${score(row.opportunity_score)} / ${score(row.financial_readiness_score)}` : score(metric)}</text>` : ''}</g>`;
}

function drawCurve(metric, context) {
  const left=40, top=27, width=394, height=182, bottom=top+height;
  const selectedIndex = model.scenarios.findIndex(s => s.id === state.scenario);
  const x = i => left + i * width / (model.scenarios.length-1);
  const y = n => pointY(n,top,height);
  let svg=`<svg viewBox="0 0 474 253" role="group" aria-labelledby="${context}-title ${context}-description"><title id="${context}-title">${context==='os'?'Opportunity Score':'Financial Readiness Score'} across six independent scenarios</title><desc id="${context}-description">Illustrative simulation, not measured results. Points can be selected with a mouse, touch, Enter or Space. Scores run from zero to one hundred.</desc>`;
  svg += `<rect x="${Math.max(22,x(selectedIndex)-20)}" y="17" width="40" height="210" rx="7" fill="#edf6f2"/>`;
  for (const tick of [0,25,50,75,100]) svg+=`<line class="plot-grid" x1="${left}" x2="${left+width}" y1="${y(tick)}" y2="${y(tick)}"/><text class="plot-tick" x="31" y="${y(tick)+4}" text-anchor="end">${tick}</text>`;
  model.scenarios.forEach((s,i)=>{svg+=`<text class="plot-tick" x="${x(i)}" y="232" text-anchor="middle" font-weight="${s.id===state.scenario?'750':'400'}">${s.id}</text>`;});
  policies.forEach((p,i)=>{
    const points=model.scenarios.map(s=>rowFor(s.id,i));
    const opacity=state.policy===null||state.policy===i?1:.26;
    svg+=`<g opacity="${opacity}"><polyline class="series-line" stroke="${p.color}" stroke-dasharray="${p.dash}" points="${points.map((r,j)=>`${x(j)},${y(r[metric])}`).join(' ')}"/>`;
    points.forEach((r,j)=>{svg+=pointMarkup(r,i,x(j),y(r[metric]),context,state.policy===i&&r.scenario===state.scenario);});
    svg+='</g>';
  });
  return svg+'</svg>';
}

function drawMap() {
  const left=59, top=28, width=414, height=290;
  const x=n=>left+n/100*width, y=n=>pointY(n,top,height);
  let svg='<svg viewBox="0 0 530 370" role="group" aria-labelledby="map-svg-title map-description"><title id="map-svg-title">Opportunity versus Financial Readiness</title><desc id="map-description">Three policy positions for the selected synthetic scenario. Horizontal axis is Opportunity Score; vertical axis is FRS. No Business Play classification or validated threshold is implied.</desc>';
  for(const tick of [0,25,50,75,100]) svg+=`<line class="plot-grid" x1="${left}" x2="${left+width}" y1="${y(tick)}" y2="${y(tick)}"/><line class="plot-grid" x1="${x(tick)}" x2="${x(tick)}" y1="${top}" y2="${top+height}"/><text class="plot-tick" x="45" y="${y(tick)+4}" text-anchor="end">${tick}</text><text class="plot-tick" x="${x(tick)}" y="340" text-anchor="middle">${tick}</text>`;
  svg+='<text class="axis-title" x="264" y="363" text-anchor="middle">Opportunity Score →</text><text class="axis-title" x="16" y="175" text-anchor="middle" transform="rotate(-90 16 175)">Financial Readiness Score →</text>';
  selectedRows().forEach((r,i)=>{
    const opacity=state.policy===null||state.policy===i?1:.26;
    svg+=`<g opacity="${opacity}">${pointMarkup(r,i,x(r.opportunity_score),y(r.financial_readiness_score),'map',state.policy===i)}</g>`;
  });
  return svg+'</svg>';
}

function updateControls() {
  byId('scenario-buttons').innerHTML=model.scenarios.map(s=>`<button data-scenario="${s.id}" aria-pressed="${state.scenario===s.id}" aria-controls="explorer"><b>${s.id}</b>${esc(s.label)}</button>`).join('');
  byId('policy-buttons').innerHTML=`<button data-policy="all" aria-pressed="${state.policy===null}" style="--tone:#142c40">All approaches</button>`+policies.map((p,i)=>`<button data-policy="${i}" aria-pressed="${state.policy===i}" style="--tone:${p.color}"><i aria-hidden="true"></i>${esc(p.short)}</button>`).join('');
  document.querySelectorAll('[data-view]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.view===state.view)));
}

function render() {
  const s=model.scenarios.find(s=>s.id===state.scenario);
  updateControls();
  byId('scenario-title').textContent=`${s.id} · ${s.label}`;
  byId('scenario-money').textContent=`Starting cash ${money(s.cash)} · Current liabilities ${money(s.current_liabilities)}`;
  byId('os-plot').innerHTML=drawCurve('opportunity_score','os');
  byId('frs-plot').innerHTML=drawCurve('financial_readiness_score','frs');
  byId('map-plot').innerHTML=drawMap();
  byId('curves-panel').hidden=state.view!=='curves';
  byId('map-panel').hidden=state.view!=='map';
  const rows=selectedRows();
  byId('policy-cards').innerHTML=rows.map((r,i)=>`<article class="policy-card ${state.policy===i?'is-selected':''}" style="--tone:${policies[i].color}" data-card-policy="${i}"><h3>${esc(i===0?policies[i].short:policies[i].name)}</h3><div class="card-scores"><div><b>${score(r.opportunity_score)}</b><small>Opportunity</small></div><div><b>${score(r.financial_readiness_score)}</b><small>FRS</small></div></div><dl><div><dt>Cash committed</dt><dd>${money(r.spend_thb)}</dd></div><div><dt>Cash remaining</dt><dd>${money(r.cash_after_purchase_thb)}</dd></div><div><dt>Planned gross profit</dt><dd>${money(r.planned_gross_profit_thb)}</dd></div></dl></article>`).join('');
  if(state.policy!==null){
    const r=rows[state.policy];
    byId('insight').textContent=`${policies[state.policy].short} commits ${money(r.spend_thb)} and leaves ${money(r.cash_after_purchase_thb)}. Its scores: Opportunity ${score(r.opportunity_score)} / FRS ${score(r.financial_readiness_score)}.`;
  }else{
    const bestOS=Math.max(...rows.map(r=>r.opportunity_score));
    const bestFRS=Math.max(...rows.map(r=>r.financial_readiness_score));
    const names=key=>rows.map((r,i)=>({r,i})).filter(({r})=>Math.abs(r[key]-(key==='opportunity_score'?bestOS:bestFRS))<1e-8).map(({i})=>policies[i].short).join(' and ');
    byId('insight').textContent=`${names('opportunity_score')} has the highest opportunity score (${score(bestOS)}); ${names('financial_readiness_score')} retains the highest FRS (${score(bestFRS)}). Select an approach to inspect its cash commitment.`;
  }
  hideTooltip();
}

function selectScenario(id) {
  if(!model.scenarios.some(s=>s.id===id))return;
  state.scenario=id;render();
}
function selectPolicy(index) {
  if(index!==null&&(!Number.isInteger(index)||index<0||index>=policies.length))return;
  state.policy=index;render();
}
function setView(view) {
  if(!['curves','map'].includes(view))return;
  state.view=view;render();
}
function hideTooltip(){byId('tooltip').hidden=true;}
function showTooltip(point) {
  const p=Number(point.dataset.policy), r=rowFor(point.dataset.scenario,p);
  const tip=byId('tooltip');
  tip.textContent=`${r.scenario} · ${policies[p].short} — OS ${score(r.opportunity_score)} / FRS ${score(r.financial_readiness_score)} · Cash left ${money(r.cash_after_purchase_thb)}`;
  tip.hidden=false;
  const panel=byId('explorer').getBoundingClientRect(), rect=point.getBoundingClientRect();
  const tipWidth=tip.getBoundingClientRect().width || 240;
  tip.style.left=Math.max(8,Math.min(rect.left-panel.left,panel.width-tipWidth-12))+'px';
  tip.style.top=Math.max(6,rect.top-panel.top-tip.getBoundingClientRect().height-10)+'px';
}
function activatePoint(point,restoreFocus=false) {
  const id=point.id;
  state.scenario=point.dataset.scenario;state.policy=Number(point.dataset.policy);render();
  if(restoreFocus){const target=byId(id);if(target)target.focus();}
}

byId('scenario-buttons').addEventListener('click',e=>{
  const button=e.target.closest('button[data-scenario]');if(!button)return;
  const id=button.dataset.scenario;selectScenario(id);
  byId('scenario-buttons').querySelector(`[data-scenario="${id}"]`).focus();
});
byId('policy-buttons').addEventListener('click',e=>{
  const button=e.target.closest('button[data-policy]');if(!button)return;
  const p=button.dataset.policy;selectPolicy(p==='all'?null:Number(p));
  byId('policy-buttons').querySelector(`[data-policy="${p}"]`).focus();
});
document.querySelectorAll('[data-view]').forEach(button=>button.addEventListener('click',()=>setView(button.dataset.view)));
byId('explorer').addEventListener('click',e=>{const p=e.target.closest('.point');if(p)activatePoint(p);});
byId('explorer').addEventListener('keydown',e=>{
  if(e.key==='Escape'){hideTooltip();return;}
  const p=e.target.closest('.point');
  if(p&&(e.key==='Enter'||e.key===' ')){e.preventDefault();activatePoint(p,true);}
});
byId('explorer').addEventListener('pointerover',e=>{const p=e.target.closest('.point');if(p)showTooltip(p);});
byId('explorer').addEventListener('focusin',e=>{const p=e.target.closest('.point');if(p)showTooltip(p);});
byId('explorer').addEventListener('pointerout',e=>{if(e.target.closest('.point'))hideTooltip();});
byId('explorer').addEventListener('focusout',e=>{if(e.target.closest('.point'))hideTooltip();});

render();
document.documentElement.classList.add('has-js');
/* The pictures preserve the original public demo. These controls only change the visible panel. */
(() => {
  const tabs = [...document.querySelectorAll('[data-step]')];
  const select = (tab, focus = false) => {
    tabs.forEach(candidate => {
      const active = candidate === tab;
      candidate.setAttribute('aria-selected', String(active));
      candidate.tabIndex = active ? 0 : -1;
      document.getElementById(candidate.getAttribute('aria-controls')).hidden = !active;
    });
    if (focus) tab.focus();
  };
  tabs.forEach((tab, index) => {
    tab.addEventListener('click', () => select(tab));
    tab.addEventListener('keydown', event => {
      const next = event.key === 'ArrowRight' ? (index + 1) % tabs.length
        : event.key === 'ArrowLeft' ? (index + tabs.length - 1) % tabs.length
        : event.key === 'Home' ? 0 : event.key === 'End' ? tabs.length - 1 : null;
      if (next === null) return;
      event.preventDefault(); select(tabs[next], true);
    });
  });
  if (tabs.length) select(tabs[0]);
})();
(() => {
  const viewer = document.getElementById('image-viewer');
  if (!viewer || !viewer.showModal) return;
  const photo = document.getElementById('viewer-image');
  const caption = document.getElementById('viewer-caption');
  const original = document.getElementById('viewer-original');
  let trigger;
  document.querySelectorAll('a[data-lightbox]').forEach(link => {
    link.addEventListener('click', event => {
      if (event.ctrlKey || event.metaKey || event.shiftKey || event.altKey) return;
      event.preventDefault(); trigger = link;
      const image = link.querySelector('img');
      photo.src = link.href; photo.alt = image.alt;
      caption.textContent = link.closest('figure').querySelector('figcaption').textContent.replace('Select the image to enlarge.', '').trim();
      original.href = link.href;
      viewer.showModal();
    });
  });
  document.getElementById('close-viewer').addEventListener('click', () => viewer.close());
  viewer.addEventListener('click', event => { if (event.target === viewer) viewer.close(); });
  viewer.addEventListener('close', () => { if (trigger) trigger.focus(); });
})();
(() => {
  const urls = new Map();
  document.querySelectorAll('a[href^="data:"]').forEach(link => {
    const value = link.getAttribute('href');
    if (!urls.has(value)) {
      const comma = value.indexOf(',');
      const mime = value.slice(5, value.indexOf(';'));
      const bytes = Uint8Array.from(atob(value.slice(comma + 1)), c => c.charCodeAt(0));
      urls.set(value, URL.createObjectURL(new Blob([bytes], {type: mime})));
    }
    link.href = urls.get(value);
  });
})();